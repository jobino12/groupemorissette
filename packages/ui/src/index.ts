// shadcn primitives land here once we install the components.
// Run from apps/web: pnpm dlx shadcn@latest add button input dialog form
// Then re-export them from this package so apps/web imports from @gm/ui.
export {};
